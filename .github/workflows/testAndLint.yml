name: testAndLint
on: push
jobs:
  linting:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: setup all dependencies
        run: composer install

      - name: linting
        run: make lint
        
      - name: test
        run: make test
